<UserControl
    x:Class="EnchantedMirror.Modules.Clock.DigitalClock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EnchantedMirror.Modules.Clock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="270">

    <Grid>
        <RelativePanel Height="64" MinWidth="270">
            <TextBlock FontStretch="Normal"
                    Margin="0,-10,0,0"
                    Text="{x:Bind TheTime, Mode=OneWay}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    FontFamily="Segoe UI"
                    FontSize="52"
                    x:Name="time"/>
            <TextBlock FontStretch="Normal"
                     Margin="220,-2,0,0"
                     Text=""
                     Foreground="Gray"
                     FontWeight="SemiBold"
                     FontFamily="Segoe UI"
                     FontSize="32"
                     x:Name="seconds" />
        </RelativePanel>
    </Grid>
</UserControl>
